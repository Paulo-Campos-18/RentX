generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

enum RentalStatus {
  Andamento
  Terminada
}

model Users {
  id      Int      @id @default(autoincrement())
  name    String
  email   String
  rentals Rentals[]
}

model Cars {
  id         Int      @id @default(autoincrement())
  placa      String
  disponivel Boolean @default(true)
  rentals    Rentals[]
}

model Rentals {
  id        Int      @id @default(autoincrement())
  user_id   Int
  car_id    Int
  startDate DateTime @default(now())
  endDate   DateTime
  status    RentalStatus @default(Andamento)

  user Users @relation(fields: [user_id], references: [id])
  car  Cars  @relation(fields: [car_id], references: [id])
}
